# `@oss-error-registry/registry`

The committed, statically imported production detector catalog for OSS Error
Registry.

The package root exports only `builtInDetectors`. Runtime loading is
filesystem-free and does not discover or dynamically import detector modules.

See the [repository README](../../README.md) and
[registry architecture](../../docs/registry-architecture.md) for the supported
catalog and contributor workflow.
